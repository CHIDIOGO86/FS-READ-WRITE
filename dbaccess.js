const fs = require("fs")
    let data = fs.readFileSync("./database.js", "utf8"
    //fs means file system is a module in node js that gives access to the internal storage of the system. it can also write
)


console.log(data)